# Markov-Python-System
Python (QT GUI) application that can emulate and create output strings as result of the application of a series of rules and input strings based in the known "Markov algorithm".
